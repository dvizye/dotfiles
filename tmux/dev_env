selectp -t 0 # select the first (0) pane
splitw -h -p 50 # split it into two halves
send-keys -t left 'vim' C-m # start vim in first pane
selectp -t 0 # select the first (0) pane
splitw -v -p 10 # split into small console at bottom
